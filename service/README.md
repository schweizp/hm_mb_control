This are example files to run the control-script as a systemd service

To edit the service-files:
```
sudo systemctl --force --full edit hm-mb-control.service
```

or the timer file:

```
sudo systemctl --force --full edit hm-mb-control.timer
```
